/** this lets us communicate with the webkit engine to render web
 *  content into the Mika UI
 */
#include "mikaWebkit.h"

typedef struct mikaWebkit
{
} mikaWebkit;

struct mikaWebkit *mikaWebkitInit( struct mikaWebkit *dw );
